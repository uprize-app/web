import { PlaceholderCard } from "@/shared/components/PlaceholderCard";

const Loading = () => (
  <div aria-busy="true">
    <PlaceholderCard tag="불러오는 중" title="UPRIZE" />
  </div>
);

export default Loading;
